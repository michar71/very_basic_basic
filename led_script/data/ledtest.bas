NUMLED = GETMAXLED(0)

DIM LEDR(NUMLED)
DIM LEDG(NUMLED)
DIM LEDB(NUMLED)

FORMAT "LEDCOUNT: %",NUMLED

FOR R=0 TO 10
	FOR I=1 TO NUMLED
		S = SETARRAY(LEDR,1,NUMLED,0)
		LEDR(I) = 255
		S = SETLEDRGB(LEDR,LEDG,LEDB)
		S = WAIT(200)
	END FOR
END FOR
