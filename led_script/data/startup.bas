NUMLED = GETMAXLED(0)
DIM LEDR(NUMLED)
DIM LEDG(NUMLED)
DIM LEDB(NUMLED)

SUB SETCOL R,G,B
    S = SETARRAY(LEDR,1,NUMLED,R)
    S = SETARRAY(LEDG,1,NUMLED,G)
    S = SETARRAY(LEDB,1,NUMLED,B)
    S = SETLEDRGB(LEDR,LEDG,LEDB)
    S = WAIT(500)
END SUB

SETCOL 64,0,0
SETCOL 0,64,0
SETCOL 0,0,64
SETCOL 0,0,0
FORMAT "STARTUP DONE"